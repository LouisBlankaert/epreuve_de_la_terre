// affiche la racine carrée d’un entier positif
const number = 9;
if (number >= 0) {
  const racineCarré = Math.sqrt(number);
  console.log(racineCarré);
} else {
  console.log("entrer un entier positif.");
}
